import * as Animatable from 'react-native-animatable';

export const {
  View: AnimatedView,
  Image: AnimatedImage,
  Text: AnimatedText,
  createAnimatableComponent,
} = Animatable;

export default Animatable;
